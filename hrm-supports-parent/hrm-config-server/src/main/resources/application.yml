server:
  port: 1299
spring:
  application:
    name: CONFIG-SERVER
  cloud:
    config:
      server:
        git:
          uri: https://github.com/My-xq/hrm-config.git  #仓库地址
  eureka:
    client:
      serviceUrl:
        defafultZone: http://localhost:8761/eureka/
    instance:
      perfer-ip-address: true #以ip注册